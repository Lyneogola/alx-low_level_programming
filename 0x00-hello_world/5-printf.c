#include <stdio.h>
/**
 * main - prints the exact line followed by new line
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n")
	return (0);
}
