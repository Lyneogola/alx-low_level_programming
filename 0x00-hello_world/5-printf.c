#include <stdio.h>
/**
 * main - programming for creating a exact file plus execute a new line
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
